{
  "name": "root",
  "private": true,
  "scripts": {
    "dev:server": "yarn workspace @mmo/server run dev",
    "dev:client": "yarn workspace @mmo/client run dev",
    "dev": "turbo run dev",
    "build:server": "yarn workspace @mmo/server run build",
    "build:client": "yarn workspace @mmo/client run build"
  },
  "workspaces": {
    "packages": [
      "libs/*",
      "apps/*"
    ]
  },
  "devDependencies": {
    "concurrently": "^7.5.0"
  },
  "dependencies": {
    "@types/pbkdf2": "^3.1.0",
    "@types/uuid": "^9.0.1",
    "pbkdf2": "^3.1.2",
    "turbo": "^1.7.0",
    "uuid": "^9.0.0"
  }
}
